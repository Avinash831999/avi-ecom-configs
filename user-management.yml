server:
  port: 8020
spring:
  application:
    name: user-management
  datasource:
    url: jdbc:postgresql://localhost:5436/userdb
    username: userdbuser
    password: Login@123
  jpa:
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        ddl-auto: update
  # config:
  #   import:
  #     -"user-management_prod.yml"
  #     -"user-management_qa.yml"
  # profiles:
  #   active: "qa"    