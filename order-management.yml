server:
  port: 8030
spring:
  application:
    name: order-management
  datasource:
    url: jdbc:postgresql://localhost:5438/orderdb
    username: orderuser
    password: Login@123
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  # config:
  #   import:
  #     -"order-management_prod.yml"
  #     -"order-management_qa.yml"
  # profiles:
  #   active: "qa"
