server:
  port: 8010
spring:
  application:
    name: product-management
  datasource:
    url: jdbc:postgresql://productdb.cn8ceqos04go.eu-north-1.rds.amazonaws.com:5432/ProductDB
    username: postgres
    password: Login#1234
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  # config:
  #   import:
  #     -"product-management_prod.yml"
  #     -"product-management_qa.yml"
  # profiles:
  #   active: "qa"
